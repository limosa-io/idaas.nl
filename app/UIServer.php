<?php

namespace App;

use App\Model;

class UIServer extends Model
{
    // Popup toestaan bij redirect_uri lijst.

    /* Each uiserver has an url, ui display settings. And there must be a default ... ? */

    // Display UI server in iframe met transparent background by default.
    // Load UI Settings from parent window.

    // Connect clients aan UIServer. Dan die UIServer gebruiken. Op client een (nullable) ui_server_id property maken.
}
